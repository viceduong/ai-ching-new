name: Build iOS App

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-ios:
    name: Build AiChing (iOS)
    runs-on: macos-14  # M1 macOS runner with Xcode 15

    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer

      - name: Install XcodeGen
        run: |
          brew install xcodegen
          xcodegen --version

      - name: Generate Xcode Project
        run: |
          cd ios
          # Generate with format compatible with Xcode 15
          xcodegen generate --spec project.yml --use-project-version 15.0
          echo "Generated Xcode project:"
          ls -la AiChing.xcodeproj/
          # Verify format version
          python3 -c "import plistlib; p=plistlib.load(open('AiChing.xcodeproj/project.pbxproj','rb')); print('Format:', p.get('archiveVersion','unknown'))"

      - name: Build iOS App
        run: |
          cd ios
          xcodebuild \
            -project AiChing.xcodeproj \
            -scheme AiChing \
            -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' \
            -configuration Debug \
            clean build \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: AiChing-build
          path: ~/Library/Developer/Xcode/DerivedData/**/Build/**/*.app
          retention-days: 3

  build-core:
    name: Build AiChingCore (Cross-Platform)
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-14, ubuntu-latest]  # Windows included if Swift available
        # os: [macos-14, ubuntu-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Build AiChingCore
        run: |
          cd ios/AiChingCore
          swift build

      - name: Test AiChingCore
        run: |
          cd ios/AiChingCore
          swift test 2>&1
        continue-on-error: ${{ matrix.os != 'macos-14' }}

  build-windows-swift:
    name: Build on Windows (Swift)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Swift
        uses: swift-actions/setup-swift@v2
        with:
          swift-version: "5.9"

      - name: Build AiChingCore
        run: |
          cd ios/AiChingCore
          swift build
        shell: pwsh

      - name: Test AiChingCore
        run: |
          cd ios/AiChingCore
          swift test
        shell: pwsh
